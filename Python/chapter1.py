x = input("Please enter your name: ")
print(f"Hello, {x}!")

# introduced the input() function to get user input and the print() function to display a message. The f-string is used to format the output with the user's name.
#chapter1.py is a simple Python script that prompts the user to enter their name and then greets them with a personalized message. The input() function is used to capture the user's input, and the print() function is used to display the greeting. The f-string allows for easy string formatting by embedding the variable x directly within the string.
age = 8
anotherVariable = age

a = 1
b = 2

a == b #False
a != b #True
a > b #False
a <= b #True

condition1 = True
condition2 = False

not condition1 #False
condition1 and condition2 #False
condition1 or condition2 #True

#something false